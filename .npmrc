# Pin public registry for this repo so a parent/global config (e.g. corporate) does not break installs.
# If `npm install` still fails, use: npm run install:retry   or   npm run install:mirror
registry=https://registry.npmjs.org/
